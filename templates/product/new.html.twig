{% extends 'base.html.twig' %}
{% block body %}
    <main>
        {{ form_start(product_form) }}
            {{ form_widget(product_form) }}
        <input type="submit" value="submit">
        {{ form_end(product_form) }}
    </main>
{% endblock %}